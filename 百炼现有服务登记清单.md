# SoulWalking 百炼登记内容（按控制台表单填写）

将 `https://<项目公网域名>` 替换为部署后的实际 HTTPS 地址。当前本地地址 `http://127.0.0.1:8000` 只能本机调试，不能填到百炼。

---

## API 工具 1：SoulWalking 漫游路线生成

### 基本信息

| 控制台字段 | 填写内容 |
| --- | --- |
| 工具名称 * | `SoulWalking漫游路线生成` |
| 工具描述 * | `根据用户的自然语言需求，为南京老门东生成个性化 Citywalk 路线。服务会综合空间知识库、开放状态、天气、景区客流、餐饮补给和步行路线，返回推荐节点、路线时长及说明。` |
| 工具类型 * | `API` |
| 工具地址 * | `https://<项目公网域名>/api/v1/plans` |
| 请求方式 * | `POST` |

### Header

| header 名 | 取值方式 | header 值 | 描述 | 加密 | 开放修改 |
| --- | --- | --- | --- | --- | --- |
| `Content-Type` | 常量 | `application/json` | 请求体使用 JSON 格式。 | 否 | 否 |

### 请求参数

只填写以下 3 项即可构成当前接口的最小有效请求；不要把图片中没有的参数强行设为必填。

| 字段名称 | 类型 | 描述 | 默认值 | 必填 | 开放修改 |
| --- | --- | --- | --- | --- | --- |
| `user_id` | String | 匿名用户标识，用于读取项目中的偏好记忆。演示时使用固定值即可。 | `bailian-demo-user` | 是 | 否 |
| `session_id` | String | 本次对话的会话标识，用于保存短期上下文。演示时使用固定值即可。 | `bailian-demo-session` | 是 | 否 |
| `query` | String | 用户想怎样在老门东漫游的自然语言需求，例如“想安静走两小时，看看有烟火气的小巷”。 |  | 是 | 是 |

### 返回参数

| 字段名称 | 类型 | 描述 |
| --- | --- | --- |
| `answer` | String | 面向用户的完整中文路线建议。 |
| `route_intro` | String | 路线开场说明。 |
| `recommendations` | Array | 推荐空间节点、标签、理由和证据。 |
| `route` | Object | 路线距离、时长、步行指引与数据来源。 |
| `weather` | Object | 天气及数据来源或降级提示。 |
| `crowd` | Object | 南京文旅定时发布的景区客流快照。 |
| `dining` | Object | 餐饮补给节点。 |
| `warnings` | Array | 数据未核验、外部服务不可用或降级等提示。 |

### 其他设置

| 控制台字段 | 填写内容 |
| --- | --- |
| 请求限制 * | `5` 次/秒 |
| 异步调用 | `不支持` |
| 流式输出 | `不支持` |

---

## API 工具 2：老门东城市情境查询

### 基本信息

| 控制台字段 | 填写内容 |
| --- | --- |
| 工具名称 * | `老门东城市情境查询` |
| 工具描述 * | `查询南京老门东出行所需的天气和南京文旅景区客流快照。客流是定时公开快照，不是秒级实时人数。` |
| 工具类型 * | `API` |
| 工具地址 * | `https://<项目公网域名>/api/v1/city-context` |
| 请求方式 * | `GET` |

### Header

无需添加 Header。

### 请求参数

无需添加请求参数。

### 返回参数

| 字段名称 | 类型 | 描述 |
| --- | --- | --- |
| `area` | String | 固定服务区域：南京·老门东。 |
| `weather` | Object | 天气、温度、数据提供方及提示。 |
| `crowd` | Object | 景区名称、人数/容量、舒适度、发布时间与数据来源。 |

### 其他设置

| 控制台字段 | 填写内容 |
| --- | --- |
| 请求限制 * | `5` 次/秒 |
| 异步调用 | `不支持` |
| 流式输出 | `不支持` |

---

## API 工具 3：SoulWalking 行走中重规划（仅在表单支持 Object / Array 参数时登记）

### 基本信息

| 控制台字段 | 填写内容 |
| --- | --- |
| 工具名称 * | `SoulWalking行走中重规划` |
| 工具描述 * | `用户在漫游途中提出想安静、休息、吃饭、缩短或延长路线等反馈时，替换尚未完成的路线尾段。` |
| 工具类型 * | `API` |
| 工具地址 * | `https://<项目公网域名>/api/v1/walk/replan` |
| 请求方式 * | `POST` |

### Header

| header 名 | 取值方式 | header 值 | 描述 | 加密 | 开放修改 |
| --- | --- | --- | --- | --- | --- |
| `Content-Type` | 常量 | `application/json` | 请求体使用 JSON 格式。 | 否 | 否 |

### 请求参数

| 字段名称 | 类型 | 描述 | 默认值 | 必填 | 开放修改 |
| --- | --- | --- | --- | --- | --- |
| `user_id` | String | 匿名用户标识。 | `bailian-demo-user` | 是 | 否 |
| `session_id` | String | 当前会话标识。 | `bailian-demo-session` | 是 | 否 |
| `feedback` | String | 用户途中反馈，例如“这里太吵了，换一段安静路线”。 |  | 是 | 是 |
| `current_position` | Object | 当前坐标对象：`longitude`、`latitude`、可选 `name`。 |  | 是 | 是 |
| `visited_space_ids` | Array | 已访问节点 ID 列表。 | `[]` | 否 | 是 |
| `remaining_space_ids` | Array | 未访问节点 ID 列表，最多 8 项。 | `[]` | 否 | 是 |

### 返回参数

| 字段名称 | 类型 | 描述 |
| --- | --- | --- |
| `action` | String | 重规划动作：quiet、rest、dining、extend、return 或 shorten。 |
| `message` | String | 面向用户的调整说明。 |
| `recommendations` | Array | 新推荐节点。 |
| `route` | Object | 新的剩余步行路线。 |
| `dining_stop` | Object | 需要就餐时的建议餐饮节点。 |
| `warnings` | Array | 降级或约束提示。 |

### 其他设置

| 控制台字段 | 填写内容 |
| --- | --- |
| 请求限制 * | `3` 次/秒 |
| 异步调用 | `不支持` |
| 流式输出 | `不支持` |

> 如果类型下拉框没有 `Object` 和 `Array`，不要登记此工具；当前项目没有把坐标和列表改为字符串接口。

---

## API 工具 4：SoulWalking 餐饮插点调整（仅在表单支持 Object / Array 参数时登记）

### 基本信息

| 控制台字段 | 填写内容 |
| --- | --- |
| 工具名称 * | `SoulWalking餐饮插点调整` |
| 工具描述 * | `当用户需要就近吃饭或补给时，查询项目内餐饮密度节点，并把首选餐饮点加入余下的步行路线。` |
| 工具类型 * | `API` |
| 工具地址 * | `https://<项目公网域名>/api/v1/route-adjustments` |
| 请求方式 * | `POST` |

### Header

| header 名 | 取值方式 | header 值 | 描述 | 加密 | 开放修改 |
| --- | --- | --- | --- | --- | --- |
| `Content-Type` | 常量 | `application/json` | 请求体使用 JSON 格式。 | 否 | 否 |

### 请求参数

| 字段名称 | 类型 | 描述 | 默认值 | 必填 | 开放修改 |
| --- | --- | --- | --- | --- | --- |
| `user_id` | String | 匿名用户标识。 | `bailian-demo-user` | 是 | 否 |
| `session_id` | String | 当前会话标识。 | `bailian-demo-session` | 是 | 否 |
| `origin` | Object | 当前起点坐标：`longitude`、`latitude`、可选 `name`。 |  | 是 | 是 |
| `remaining_destinations` | Array | 剩余目标坐标列表，最多 8 项。 | `[]` | 否 | 是 |

### 返回参数

| 字段名称 | 类型 | 描述 |
| --- | --- | --- |
| `selected_restaurant` | Object | 选中的餐饮补给节点。 |
| `dining` | Object | 餐饮候选结果和数据来源。 |
| `route` | Object | 经过餐饮点后的路线。 |
| `warning` | String | 无法获取餐饮或路线时的说明。 |

### 其他设置

| 控制台字段 | 填写内容 |
| --- | --- |
| 请求限制 * | `3` 次/秒 |
| 异步调用 | `不支持` |
| 流式输出 | `不支持` |

---

## MCP 服务：SoulWalking-高德地图MCP

### 基本信息

| 控制台字段 | 填写内容 |
| --- | --- |
| 服务名称 * | `SoulWalking-高德地图MCP` |
| 服务描述 * | `为南京老门东 Citywalk 提供地理编码、天气、步行路线和地图生成能力。仅在需要实时天气、地点解析或道路步行指引时调用。` |
| 注册模式 * | `STREAMABLE` |

### 服务配置

```json
{
  "mcpServers": {
    "soulwalking-amap": {
      "disabled": false,
      "timeout": 60,
      "url": "https://mcp.amap.com/mcp?key=<AMAP_API_KEY>",
      "transportType": "streamableHttp"
    }
  }
}
```

将 `<AMAP_API_KEY>` 替换为项目 `.env` 里的高德 **Web 服务 Key**。同时保持现有项目配置：

```dotenv
AMAP_API_KEY=<高德 Web 服务 Key>
MCP_ENABLED=true
```

不要选择 `SSE`，也不要填写 `AMAP_JS_KEY` 或 `AMAP_SECURITY_JS_CODE`；这两个值仅供网页地图使用。

---

## 当前内部工具与上述登记项的对应关系

| 当前项目内部工具 | 不单独登记的原因 | 由哪个 API 工具使用 |
| --- | --- | --- |
| `search_spaces` | Python 内部 RAG 函数，无独立 URL。 | 漫游路线生成、行走中重规划 |
| `check_opening_status` | Python 内部开放时间核验函数，无独立 URL。 | 漫游路线生成 |
| `get_weather` | Python 内部函数，优先调用高德 MCP。 | 漫游路线生成、城市情境查询 |
| `get_tourism_crowd` | Python 内部函数，读取南京文旅定时快照。 | 漫游路线生成、城市情境查询 |
| `search_dining` | Python 内部函数，查询本地餐饮密度数据。 | 漫游路线生成、餐饮插点调整、行走中重规划 |
| `plan_walking_route` | Python 内部函数，优先调用高德 MCP 和高德 Web 路线。 | 漫游路线生成、餐饮插点调整、行走中重规划 |

当前项目是高德 MCP 的**客户端**，并没有自建 MCP Server；因此 MCP 登记项只有上面的高德远程服务。

---

## 百炼 v1.1 智能体配置（SoulWalking老门东漫游助手）

以下内容用于已创建的 `v1.1` 版本。先绑定知识库，再把现有的高德 MCP 转换为 Skill 并加入任务；不需要新建或复制高德 Key。

### 1. 规划页

| 项目 | 填写 / 选择 |
| --- | --- |
| 模型 | `deepseek-v4-pro` |
| 知识库 | `old-mendong-spatial-kb（老门东空间知识库）` |
| 短期记忆 | 开启 |
| 长期记忆 | 演示阶段关闭，避免把不同体验者的偏好混在一起 |
| Skills 自优化 | 关闭 |

### 2. 全局指令

将下面文字完整粘贴到“全局指令”：

```text
你是 SoulWalking 老门东漫游助手，服务南京老门东及邻近步行区域。把空间人格判断、知识库检索和高德步行规划组合为可执行 Citywalk。

【人格测评】用户说“开始测评/测性格/我适合哪里”时，用 5 个二选一问题依次判断：安静或热闹、确定或探索、效率或风景、随性或规划、对拥挤/昏暗的敏感度；每次只问 1 题。结束后给非医疗性质的空间人格标签、3 个偏好和适合的漫游节奏。

【路线规划】优先读取已有空间人格；确认出发点、时长、同行者、步行强度、偏好和限制。缺少信息最多问 3 个问题；用户要求直接给方案时，采用并写明默认值。

【知识与工具】推荐地点前先检索知识库，以实测节点与待核验标记为准。涉及天气、地点搜索、步行导航、距离/时长、附近餐饮时调用高德地图 MCP，按实际步行顺序组织路线。工具不可用时说明原因，改用预设路线；不得编造实时数据、营业状态或道路距离。

【输出】先一句结论；再写空间人格/本次偏好；编号给路线（名称、停留建议、适配原因、下一段步行信息）；最后给出发前提醒和备选路线。识别命令：开始测评、给我规划路线、安静路线、吃喝路线、适合拍照、重新规划、附近有什么、今天适合去吗。
```

### 3. 将高德 MCP 加入 Skill

在“Skills Hub”点击“安装 Skill”，选择 `工具转化(API/MCP)`，选择已有服务 `高德地图（Streamable）`，填写：

| 字段 | 填写内容 |
| --- | --- |
| Skill 备注名称 | `高德地图 Citywalk 工具` |
| 调用名称 | `amap-citywalk-tools` |
| 调用描述 | `用于查询天气、地点/POI、地理编码、步行路线、距离和附近餐饮。仅当用户需要实时出行信息时调用。` |

确认安装后回到智能体“任务”区域，点击 `新增 Skill` 并添加该 Skill，最后点击页面底部“确定”保存版本。

### 4. 调试验收

在右侧对话调试中依次输入：

1. `开始测评`
2. 依次回答：`安静`、`探索`、`风景`、`随性`、`对拥挤敏感`
3. `给我规划路线：从老门东牌坊出发，90分钟，独自，想安静又想看人文，避开拥挤。`

合格表现：第一句只问第一个二选一问题；五题后给出空间人格和偏好；路线规划会引用知识库节点，并在需要步行数据时调用高德工具。确认调试结果正确后，再点击“发布”。
