# SoulWalking 交付运行说明

这是可直接交给其他 Agent 或协作者运行的本地演示包。

## 运行

1. Windows 安装 Python 3.12 x64，并在安装时勾选 `Add Python to PATH`。
2. 解压后双击 `一键启动.bat`。
3. 首次运行会在包内创建 `.venv` 并安装依赖；完成后自动打开本地网页。

当前项目只保留一套正式前端，服务启动后访问 `http://127.0.0.1:8000/` 即可；页面通过同源 `/api/v1/plans`、`/api/v1/feedback` 接入 FastAPI 后端。

也可由 Agent 在包根目录执行：

```powershell
py -3.12 -m venv .venv
.venv\Scripts\python -m pip install -e .
.venv\Scripts\python desktop_launcher.py
```

## 已包含的配置

- `.env` 已配置高德 Web 端地图 Key、安全密钥和 Web 服务 Key。
- 二选一测试图片位于 `web/assets/questions/`，均来自项目方提供的 20 张图片压缩包。
- 37 个实测节点数据与实拍素材已经包含。
- 为避免首次运行下载本地向量模型，交付包使用 `EMBEDDING_BACKEND=hash`；高德地图、真实道路路线、天气和餐饮接口仍可调用。

## 可选：启用模型说明

如需 AI 生成更自然的路线说明，在 `.env` 中补充 `DEEPSEEK_API_KEY`。不要把该文件上传到公开仓库或公开网盘。

## 高德地图排查

若网页仍显示地图加载失败，请在高德控制台确认 Web 端 Key 允许本地调试来源（`localhost` 和 `127.0.0.1`）。高德 MCP 未配置时，系统会自动使用已配置的高德 Web API 获取步行路线、天气和餐饮。
